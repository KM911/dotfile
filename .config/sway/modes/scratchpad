#
# Scratchpad:
#
# Sway has a "scratchpad", which is a bag of holding for windows.
# You can send windows there and get them back later.

## Action // Move window to scratchpad ##
#$bindsym $mod+Shift+p move scratchpad, exec "waybar-signal scratchpad"
#  [app_id="r_scratchpad"]

# org.wezfurlong.wezterm

# $bindsym $mod+p             exec ~/.config/sway/scripts/suckless.sh f1, exec 'swaymsg [app_id="f1"] scratchpad show' , exec "waybar-signal scratchpad"

# $bindsym $mod+p exec ~/.config/sway/scripts/wezterm.sh

# $bindsym $mod+p             exec ~/.config/sway/scripts/suckless.sh f1, exec 'swaymsg [app_id="f1"] scratchpad show' , exec "waybar-signal scratchpad"
# $bindsym $mod+bracketleft   exec ~/.config/sway/scripts/suckless.sh f2, exec 'swaymsg [app_id="f2"] scratchpad show' , exec "waybar-signal scratchpad"
# $bindsym $mod+bracketright  exec ~/.config/sway/scripts/suckless.sh f3, exec 'swaymsg [app_id="f3"] scratchpad show' , exec "waybar-signal scratchpad"

$bindsym $mod+m             exec ~/.config/sway/scripts/spotify.sh  , exec 'swaymsg [class="Spotify"] scratchpad show' , exec "waybar-signal scratchpad" 

# only one is not enough for me 


$bindsym $mod+Shift+p  move scratchpad

# $bindsym $mod+p  exec ~/.config/sway/scripts/spotify.sho

# If there are multiple scratchpad windows, this command cycles through them.
## Action // Toggle scratchpad ##
# $bindsym $mod+p scratchpad show, exec "waybar-signal scratchpad"